//
// Created by kani3camp on 2024/11/09.
//


#include <iostream>
#include <string>
#include <algorithm>
#include <vector>
#include <set>
#include <cmath>
#include <climits>
#include <queue>
#include <numeric>

using namespace std;

int main()
{
    int N, M; cin >> N >> M;
    vector<int> D(N), C(M);
    for (int i = 0; i < N; i++) cin >> D[i];
    for (int i = 0; i < M; i++) cin >> C[i];

    vector dp = vector(N, vector(M, 0));

    for (int day = 0; day < N; day++) {
        for (int )
    }

    return 0;
}